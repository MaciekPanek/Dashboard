function Booking({ booking }) {
  console.log(booking);

  const {
    arrivalDate,
    departureDate,
    guestNum,
    id,
    notes,
    cost,
    Villas,
    Guests,
  } = booking;

  return <div></div>;
}

export default Booking;
